package maths;

import maths.Vector3D;
import maths.Ray;

public class GeoShape {
	// This should return a Vector3D to the intersection point. Return null if there is no intersection
	public Vector3D getIntersection(Ray ray) {
		return null;
	}
}
